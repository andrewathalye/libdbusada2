
project Tests extends "shared" is
   for Source_Dirs use ("../src/tests");
   for Object_Dir use "../obj/" & Build_Mode & "/tests";
   for Exec_Dir use "..";
   for Main use ("tests.adb");
end Tests;
